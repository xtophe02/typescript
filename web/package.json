{
  "devDependencies": {
    "typescript": "^3.8.3",
    "concurrently": "^5.2.0",
    "json-server": "^0.16.1"
  },
  "dependencies": {
    "axios": "^0.19.2"
  },
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel index.html",
    "start": "concurrently npm:start:*"
  }
}
